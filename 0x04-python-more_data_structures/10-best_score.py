#!/usr/bin/python3
def best_score(a_dictionary):
    if (a_dictionary):
        best = max(a_dictionary)
        return (best)
    elif (a_dictionary == []):
        return (None)
    else:
        return (None)
